import std.prelude;

fn defaultPlayer() -> Player {
    return Player {
        uuid: gval("UUID", .Default)
    }
}

fn testPlayer() {
    var player = defaultPlayer();
    player.damage(5);
    val name = player.getName();

    emit `player_action "SendMessage" args($name$) tags(..)`;
}