package main;

import std.prelude;

fn getAllPlayers() -> List<Player> {
    val uuids = gval("Plot Player UUIDs");
    var players: List<Player> = List.of();

    for (val uuid in uuids) {
        players.append(Player {
            uuid: uuid
        });
    }

    return players;
}

fn testPlayer() {
    for (var player in getAllPlayers()) {
        player.damage(5);
    }
}

